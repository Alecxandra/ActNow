<% if remotipart_submitted? %>
  $('#post').html("<%= j "#{render(partial: 'profiles/posts', locals: { profile: @post.profile, comment: Comment.new })}" %>")
<% else %>
  $('#post').html("<%= j render(partial: 'profiles/posts', locals: { profile: @post.profile, comment: Comment.new }) %>")
<% end %>